<?php

/**
 * افتراضات التشغيل المعتمدة.
 *
 * ⚠️ هذه قيم افتراضية اجتهادية اتُّخذت لتشغيل النظام دون انتظار حسم العميل
 * (§القسم الخامس من وثيقة النطاق). كل قيمة هنا قابلة للتعديل، وموضع
 * تعديلها مذكور بجانبها. تُراجَع مع العميل قبل الإطلاق.
 */
return [

    /*
    |--------------------------------------------------------------------------
    | بلد التشغيل والعملة والضريبة
    |--------------------------------------------------------------------------
    | الافتراض: السعودية — بناءً على أسماء الوحدات وسوق الخدمة.
    | يحدد الفوترة الضريبية وأيام نهاية الأسبوع وصيغة الجوال.
    */
    'country' => 'SA',
    'currency' => 'SAR',
    'currency_label' => 'ريال',
    'vat_rate' => 15.0,

    /*
    |--------------------------------------------------------------------------
    | نهاية الأسبوع
    |--------------------------------------------------------------------------
    | الافتراض: الجمعة والسبت. إن اعتبر العميل الخميس ليلة نهاية أسبوع
    | (شائع في الشاليهات) يُضاف 4 إلى المصفوفة.
    | 0=الأحد … 4=الخميس، 5=الجمعة، 6=السبت
    */
    'weekend_days' => [5, 6],

    /*
    |--------------------------------------------------------------------------
    | الحجوزات
    |--------------------------------------------------------------------------
    */
    'booking' => [
        // الافتراض: 25% عربون — نسبة سائدة تحمي من الإلغاء دون تنفير العميل.
        'deposit_percent' => 25.0,

        // الافتراض: خصوصية مغلقة (§1.1 الخيار ج) — الأحوط حتى يحسم العميل
        // لكل وحدة على حدة من شاشة الوحدات.
        'default_privacy_mode' => 'exclusive',

        // مجموع أسعار الأقسام منفردة يتجاوز سعر الوحدة كاملة بهذه النسبة،
        // وإلا صار تقسيم الحجز حيلة لتخفيض الفاتورة.
        'sections_premium_percent' => 15.0,

        // التذكير قبل الموعد بهذا العدد من الأيام.
        'reminder_days_before' => 1,

        /*
        | الشاليهات — تُحجز بالليالي لا بالفترات
        |
        | الافتراض: دخول 4 عصرًا وخروج 12 ظهرًا، وهو العرف السائد في الشاليهات
        | ويترك مهلة كافية للتنظيف بين إقامتين. يُراجَع مع العميل.
        */
        'chalet' => [
            'check_in_time' => '16:00',
            'check_out_time' => '12:00',

            // سقف الإقامة الواحدة — يمنع خطأ إدخال يحجز الشاليه سنة كاملة.
            'max_nights' => 30,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | الواتساب (§3.1 و §4.4)
    |--------------------------------------------------------------------------
    | حساب الحد: 150 حجز شهريًا × 4 رسائل × 12 شهرًا + هامش 30% ≈ 9,360
    | التسعير لكل محادثة (24 ساعة) لا لكل رسالة.
    */
    'whatsapp' => [
        'annual_conversation_limit' => 9360,
        'warn_at_percent' => 80,
        'categories_allowed' => ['utility', 'authentication'],
    ],

    /*
    |--------------------------------------------------------------------------
    | الموارد البشرية والرواتب
    |--------------------------------------------------------------------------
    */
    'hr' => [
        // أجر اليوم = الراتب ÷ 30 (العرف السائد في عقود العمل).
        'month_days' => 30,

        // ساعة الإضافي = (الأساسي ÷ 30 ÷ 8) × هذا المعامل.
        'overtime_multiplier' => 1.5,
        'daily_hours' => 8,

        // التنبيه قبل انتهاء الإقامة أو الجواز أو العقد بهذه المدة.
        'document_alert_days' => 60,

        // الافتراض: 18 عاملًا كما في وثيقة النطاق.
        'expected_headcount' => 18,
    ],

    /*
    |--------------------------------------------------------------------------
    | النسخ الاحتياطي (§18)
    |--------------------------------------------------------------------------
    | النسخة تُؤخذ كل ليلة ويُحتفظ بآخر عددٍ منها. الاحتفاظ بكل نسخة يملأ
    | القرص، والاحتفاظ بواحدة يجعل خطأً اكتُشف بعد يومين غير قابل للتراجع —
    | فأسبوعان وسطٌ يغطي إجازة ويسع خطأً تأخّر اكتشافه.
    */
    'backup' => [
        'keep' => 14,

        // المجلد داخل storage/app — خارج public عمدًا: نسخة القاعدة تُنزَّل
        // بصلاحية لا برابطٍ يُخمَّن.
        'path' => 'backups',

        // mysqldump أسرع وأوثق، فإن لم يكن في المسار سقط النظام إلى مُصدِّر
        // داخلي بالـPHP — نسخةٌ أبطأ خيرٌ من لا نسخة.
        'mysqldump_path' => env('MYSQLDUMP_PATH', 'mysqldump'),

        // ضغط الملف بعد كتابته إن توفّر امتداد zlib.
        'compress' => true,

        // أقصى حجم لملف يُرفع من الشاشة (ميجابايت). والحد الفعلي هو
        // الأصغر منه ومن upload_max_filesize و post_max_size في PHP — والشاشة
        // تعرض المحسوب منهما لا هذا الرقم وحده، لأن رفضًا من الخادم يقع قبل
        // أن يصل الطلب إلى لارافيل أصلًا.
        'upload_max_mb' => (int) env('BACKUP_UPLOAD_MAX_MB', 256),

        // مهلة الاستعادة بالثواني — ملف كبير يُنفّذ عبارةً عبارة يتجاوز
        // الثلاثين ثانية الافتراضية، وانقطاعه في المنتصف يترك قاعدةً ناقصة.
        'restore_timeout' => 900,
    ],

    /*
    |--------------------------------------------------------------------------
    | المخزون ونقطة البيع
    |--------------------------------------------------------------------------
    */
    'inventory' => [
        // منع البيع عند نفاد الرصيد — الأحوط محاسبيًا من السماح برصيد سالب.
        'block_negative_stock' => true,

        // التنبيه عند بلوغ حد إعادة الطلب.
        'low_stock_alert' => true,
    ],

];
